export declare class Debouncer<T extends any = any> {
    private func;
    private debounced;
    private debouncing;
    constructor(func: (...args: any[]) => Promise<T>);
    debounce<P extends any>(...args: P[]): Promise<T>;
}
//# sourceMappingURL=utils.d.ts.map